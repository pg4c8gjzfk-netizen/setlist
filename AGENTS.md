# 作業指示

- 複数の質問や章立てで進める際に必ず一つずつ説明し、私が結果などを報告してから次に進んでください。
- 命名規則は維持する。ただし、既存クラスを命名規則だけを理由に再命名しない。クラスの責務・対象・対応形式が変わり、名前が実態と合わなくなった場合に限り、影響範囲を確認して再命名する。
- XLSXの各ワークシートは独立した公演単位として扱う。読込・初回生成・再生成・保存・再読込のどの工程でも、演目を元シート以外へ移動または混在させてはならない。このシート境界は絶対に維持する。
- 修正作業は、実装前にGitHub Issueを日本語で登録し、`codex/`から始まる専用ブランチへ分岐して行う。
- コミットメッセージ、Issue・Pull Requestのタイトルと本文、GitHub上のコメントは日本語で記述する。
- 自動テストと必要なGUI確認が成功した後にPull Requestを作成し、問題がないことを確認してから`main`へ統合する。
- 未追跡・未コミットの無関係なファイルは、明示的な依頼がない限りステージやコミットに含めない。
